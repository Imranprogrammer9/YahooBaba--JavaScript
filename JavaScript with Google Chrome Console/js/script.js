// Google Chrome Console is the best tool for debugging JavaScript.

// For Prining, console.log() is used.

// For Array, console.log([1,2,3]). For viewing in table, console.table([1,2,3]).

// For Errors & Warnings, console.error() & console.warn().

// Time in Cosnsole, console.time() & console.timeEnd().

// Clear Console, console.clear().

// DOM Manipulation, document.getElementById('id').innerHTML; document.getElementById('id').innerText;